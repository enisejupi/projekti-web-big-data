# PostgreSQL Database Configuration
# Database for storing financial data during 84-hour collection

# Database Connection
DB_HOST=10.0.0.4
DB_PORT=5432
DB_NAME=financial_data
DB_USER=financeuser
DB_PASSWORD=Finance@2025!Secure

# Connection Pool Settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=40
DB_POOL_TIMEOUT=30

# Table Names
TABLE_MARKET_DATA=market_data
TABLE_PREDICTIONS=ml_predictions
TABLE_RECOMMENDATIONS=investment_recommendations
TABLE_CLUSTERING=cluster_analysis

# Data Retention
DATA_RETENTION_DAYS=90
BACKUP_ENABLED=true
BACKUP_INTERVAL_HOURS=6
